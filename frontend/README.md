# ReferralFlow Frontend

React + Vite frontend for the standalone referral commission demo.

Start with:
```powershell
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:5000/api` by default. Set `VITE_API_URL` if the API runs elsewhere.
